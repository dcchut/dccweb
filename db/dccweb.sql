-- dccweb schema file

CREATE TABLE posts (
    `id` INTEGER PRIMARY KEY,
    `o` INTEGER,
    `content` TEXT
);

CREATE TABLE projects (
    `id` INTEGER PRIMARY KEY,
    `o` INTEGER,
    `content` TEXT
);